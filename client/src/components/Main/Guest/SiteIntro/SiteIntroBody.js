import { Article } from "./siteIntroStyle";
import { siteIntroBody } from "../../../../utils/plainText";

const SiteIntroBody = ()=>{
    return(
        <Article>
            {siteIntroBody}
        </Article>
    );
};

export default SiteIntroBody